meta {
  name: Calculate price for ticket
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/rates/:ticketId/calculate
  body: none
  auth: inherit
}

params:path {
  ticketId: 
}

settings {
  encodeUrl: true
  timeout: 0
}
